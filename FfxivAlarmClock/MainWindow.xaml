<Window
    x:Class="FfxivAlarmClock.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FfxivAlarmClock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:FfxivAlarmClock.Models"
    mc:Ignorable="d">

    <Grid Margin="10" DataContext="{x:Bind ViewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView IsEnabled="{Binding Ready}" ItemsSource="{Binding Favorite}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" Padding="3">
                            <StackPanel Margin="10,0" Width="220">
                                <TextBlock Text="{Binding NameJp}"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding NameCn}" FontSize="10"/>
                                    <TextBlock Text="{Binding NameEn}" FontSize="10" Margin="5,0"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Width="180">
                                <TextBlock Text="{Binding MapJp}"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding MapCn}" FontSize="10"/>
                                    <TextBlock Text="{Binding MapEn}" FontSize="10" Margin="5,0"/>
                                </StackPanel>
                            </StackPanel>
                            <TextBlock Margin="10,0" Text="{Binding TimeSpan}"/>
                        </StackPanel>
                        <ProgressBar Value="{Binding Value}" Maximum="{Binding Maximum}"
                                     Grid.Column="1" Height="12"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock>游戏时间</TextBlock>
            <TextBlock Text="{Binding EorzeaTimeExp}" FontSize="26" Margin="0,0,0,10"/>
            <TextBlock>本地时间</TextBlock>
            <TextBlock Text="{Binding LocalTime}" FontSize="22"/>
            <TextBlock Text="{Binding LocalDate}" TextAlignment="Right"/>
        </StackPanel>
        <Expander Grid.Row="1" Grid.ColumnSpan="2" Header="List" HorizontalAlignment="Stretch">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="400"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>检索</TextBlock>
                </StackPanel>
                <ListView Grid.Row="1" ItemsSource="{Binding Table}" IsEnabled="{Binding Ready}">
                    <ListView.Resources>
                        <model:JobConverter x:Key="jc"/>
                    </ListView.Resources>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Padding="3">
                                <CheckBox IsChecked="{Binding Checked, Mode=TwoWay}" Width="60"/>
                                <StackPanel Margin="10,0" Width="220">
                                    <TextBlock Text="{Binding NameJp}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding NameCn}" FontSize="10"/>
                                        <TextBlock Text="{Binding NameEn}" FontSize="10" Margin="5,0"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Width="180">
                                    <TextBlock Text="{Binding MapJp}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding MapCn}" FontSize="10"/>
                                        <TextBlock Text="{Binding MapEn}" FontSize="10" Margin="5,0"/>
                                    </StackPanel>
                                </StackPanel>
                                <TextBlock Margin="10,0" Text="{Binding TimeSpan}"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding Version}" FontSize="10"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Job, Converter={StaticResource jc}}"/>
                                        <TextBlock Text="{Binding Level}" Margin="10,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBox Grid.Row="2"/>
            </Grid>
        </Expander>
    </Grid>
</Window>
